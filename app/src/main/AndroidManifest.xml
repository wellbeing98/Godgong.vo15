<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.basicdemoapp">

    <application
        android:allowBackup="true"
        android:extractNativeLibs="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity
            android:name=".WaitActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="com.terminaldevelopers.myzoom.intent.action.JoinBeforeHost"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".WritingStudyActivity"/>

        <activity android:name=".DetailActivity"/>
        <activity android:name=".StartActivity"/>
        <activity android:name=".ChooseRoom"/>
        <activity android:name=".ChatActivity"/>
        <activity android:name=".RegisterActivity"/>
        <activity android:name=".WritingProjectActivity"/>
        <activity android:name=".ProjectActivity"/>
        <activity android:name=".WritingChatActivity"/>
        <activity android:name=".ZoomActivity"/>
        <activity android:name=".MainActivity1"/>
    </application>

</manifest>